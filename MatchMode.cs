namespace BotUtilities.ImageProcessing
{
    // Relativ zum gesuchten bild kann eine position zurückgegeben werden

    public enum MatchMode
    {
        None,
        TopLeft,
        TopRight,
        BottomLeft,
        BottomRight,
        Mid
    }
}
